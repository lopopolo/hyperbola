#!/usr/bin/env python

import os
import sys

from dotenv import dotenv_values


def main(args):
    env = dotenv_values()
    cmd_env = os.environ.copy()
    cmd_env.update(env)
    command, *_ = args
    os.execvpe(command, args, cmd_env)


if __name__ == "__main__":
    main(sys.argv[1:])
